services:
  - type: web
    name: prospectiq
    env: python
    plan: free

    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium

    startCommand: gunicorn app:app

    envVars:
      - key: GROQ_API_KEY
        sync: false